<p style="color: green">
  <%= notice %>
</p>

<%= render @story %>

  <div>
    <% if current_user==@story.user %>
      <%= link_to "Edit this story" , edit_story_path(@story) %> |
        <%= button_to "Destroy this story" , @story, data: { turbo_confirm: "Are you sure?", turbo_method: :delete } %>
          <% end %>

            <%= link_to "Back to stories" , stories_path %>
  </div>