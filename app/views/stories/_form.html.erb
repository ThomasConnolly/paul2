<%= simple_form_for(@story) do |f| %>


  <div class="form-inputs">
    <%= f.text_field :title %>
      <%= f.rich_text_area :body %>

  </div>

  <div class="form-actions">
    <%= f.submit "Submit"  %>
  </div>
  <% end %>